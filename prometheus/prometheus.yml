global:
  scrape_interval: 15s # Cada cuánto tiempo Prometheus le pedirá datos a tu app

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus' # La ruta que habilitamos en Java
    static_configs:
      - targets: ['app-spring:8080']